# Архитектура проекта

Данный документ описывает архитектуру и структуру проекта для создания Telegram-бота по генерации контента для НКО.

## Обзор проекта

Проект представляет собой Python-приложение, построенное на принципах Clean Architecture с разделением на слои:
- **Infrastructure** (Инфраструктура) - внешние интеграции и технические детали
- **Services** (Сервисы) - бизнес-логика приложения
- **Bot** (Бот Telegram) - пользовательский интерфейс

Приложение использует **YandexGPT** для генерации контента и **Playwright** для создания визуальных карточек.

## Структура проекта

```
hackathon/
├── src/
│   ├── main.py               # Точка входа приложения (async run bootstrap)
│   ├── app.py                # Создание бота и диспетчера (aiogram)
│   ├── bootstrap.py          # Инициализация всех сервисов и зависимостей
│   ├── config.py             # Конфигурация приложения (Pydantic Settings)
│   │
│   ├── bot/                  # Слой Telegram бота
│   │   ├── main.py           # Альтернативная точка входа для тестирования
│   │   ├── states.py         # Состояния FSM для всех процессов
│   │   ├── utils.py          # Утилиты бота
│   │   ├── handlers/         # Обработчики команд и событий
│   │   │   ├── __init__.py   # Объединение всех роутеров
│   │   │   ├── start.py      # Стартовая команда и приветствие
│   │   │   ├── ngo_info.py   # Управление информацией о НКО
│   │   │   ├── questionnaire.py # Анкетирование пользователя
│   │   │   ├── generation.py # Генерация контента и карточек
│   │   │   ├── refactoring.py # Редактирование существующего контента
│   │   │   ├── content_plan.py # Создание контент-плана
│   │   │   ├── callbacks.py  # Callback обработчики
│   │   │   ├── fallback.py   # Обработчики ошибок и fallback
│   │   │   └── callbacks.py  # Inline клавиатуры
│   │   └── keyboards/        # Клавиатуры для бота
│   │       ├── __init__.py
│   │       ├── inline.py     # Inline клавиатуры
│   │       └── reply.py      # Reply клавиатуры
│   │
│   ├── infrastructure/       # Инфраструктурный слой
│   │   ├── __init__.py
│   │   ├── gpt.py            # Интеграция с YandexGPT (абстрактные интерфейсы)
│   │   ├── prompt_builder.py # Построитель промптов для разных задач
│   │   ├── response_processor.py # Обработчик ответов GPT
│   │   ├── card_generation.py # Генерация карточек (Playwright)
│   │   ├── database.py       # Управление базой данных (SQLAlchemy)
│   │   └── repositories/     # Слой доступа к данным
│   │       ├── __init__.py
│   │       ├── models/       # Модели данных
│   │       │   ├── __init__.py
│   │       │   └── ngo.py    # Модель НКО (SQLAlchemy ORM)
│   │       └── ngo_repository.py # Репозиторий НКО
│   │
│   ├── services/            # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── content_generation.py # Сервис генерации текстового контента
│   │   ├── card_generation.py # Сервис генерации визуальных карточек
│   │   └── ngo_service.py   # Сервис для работы с данными НКО
│   │
│   └── templates/           # HTML шаблоны для карточек
│       ├── telegram_post.html
│       └── universal_card.html
│
├── .env.example             # Пример переменных окружения
├── .gitignore              # Игнорируемые файлы
├── pyproject.toml          # Конфигурация проекта и зависимостей
├── README.md               # Документация проекта
└── uv.lock                 # Зависимости проекта
```

## Архитектурные принципы

### Clean Architecture

Проект построен на принципах Clean Architecture, что обеспечивает:

- **Независимость от фреймворков**: Бизнес-логика не зависит от внешних библиотек
- **Тестируемость**: Бизнес-правила могут тестироваться без UI, базы данных, веб-сервера
- **Независимость от UI**: UI может изменяться без изменения бизнес-логики
- **Независимость от базы данных**: Бизнес-логика не привязана к конкретной БД
- **Изоляция внешних систем**: Инфраструктурный код изолирован от бизнес-логики

### Слои архитектуры

#### 1. Bot Layer (Слой бота)
- **Назначение**: Взаимодействие с пользователем через Telegram API
- **Содержимое**: Обработчики команд, клавиатуры, состояния FSM
- **Зависимости**: Зависит от всех остальных слоев
- **Технологии**: aiogram 3.x

#### 2. Services Layer (Слой сервисов)
- **Назначение**: Бизнес-логика приложения
- **Содержимое**: Сервисы генерации контента, работы с НКО, карточек
- **Зависимости**: Зависит от Infrastructure (в т. ч. Repositories, Models)
- **Паттерны**: Dependency Injection, Factory, Template Method

#### 3. Infrastructure Layer (Инфраструктурный слой)
- **Назначение**: Внешние интеграции и технические детали
- **Содержимое**: YandexGPT интеграция, генерация карточек, БД
- **Паттерны**: Adapter, Factory, Dependency Injection

## Детальное описание компонентов

### Точка входа приложения

#### main.py
```python
import asyncio
import logging
from bootstrap import bootstrap
from bot.app import bot, dp

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",
)

if __name__ == "__main__":
    asyncio.run(bootstrap(bot, dp))
```

**Особенности:**
- Минимальная точка входа
- Делегирует всю инициализацию в bootstrap
- Асинхронный запуск через asyncio.run

#### app.py
```python
from aiogram import Bot, Dispatcher
from config import config

bot = Bot(token=config.BOT_TOKEN)
dp = Dispatcher()
```

**Особенности:**
- Создание основных объектов бота
- Получение токена из конфигурации
- Готовность к добавлению middleware

### Конфигурация

#### Config Class
```python
class Config(BaseSettings):
    BOT_TOKEN: str = Field(default="", env="BOT_TOKEN")
    
    # YandexGPT настройки
    YANDEXGPT_API_KEY: str = Field(default="", env="YANDEXGPT_API_KEY")
    YANDEXGPT_CATALOG_ID: str = Field(default="", env="YANDEXGPT_CATALOG_ID")
    YANDEXGPT_MODEL: str = Field(default="yandexgpt-5.1", env="YANDEXGPT_MODEL")
    YANDEXGPT_TEMPERATURE: float = Field(default=0.5, env="YANDEXGPT_TEMPERATURE")
    YANDEXGPT_MAX_TOKENS: int = Field(default=2000, env="YANDEXGPT_MAX_TOKENS")
    YANDEXGPT_API_URL: str = "https://llm.api.cloud.yandex.net/foundationModels/v1/completion"
    
    # Таймауты
    YANDEXGPT_TIMEOUT: int = 30  # секунд
    PLAYWRIGHT_TIMEOUT: int = 60000  # 60 секунд
    
    # Размеры для соцсетей
    SOCIAL_MEDIA_SIZES: Dict[str, Dict[str, Dict[str, int]]] = {
        "📱 ВКонтакте (для молодежи)": {"post": {"width": 510, "height": 510}},
        "💬 Telegram (для взрослых/бизнеса)": {"post": {"width": 1200, "height": 630}},
        "🌐 Сайт/новостная рассылка": {"og": {"width": 1200, "height": 630}}
    }
```

**Ключевые особенности:**
- Использует Pydantic Settings для типизированной конфигурации
- Поддержка переменных окружения
- Валидация настроек через `validate_config()`
- Настройки для разных социальных платформ

### Инициализация и Dependency Injection

#### bootstrap.py
```python
async def bootstrap(bot: Bot, dispatcher: Dispatcher):
    # Подключение роутеров
    dp.include_router(router)
    
    # Регистрация lifecycle событий
    dp.startup.register(on_startup)
    dp.shutdown.register(on_shutdown)
    
    # Инициализация всех сервисов
    await build_services(bot, dispatcher)
    
    # Запуск polling
    await dp.start_polling(bot)
```

**Основные функции:**
- `build_services()` - создание и внедрение всех сервисов
- `build_yandexgpt_content_generation_service()` - создание сервиса генерации текста
- `build_playwright_card_generation_service()` - создание сервиса генерации карточек
- `on_startup()` / `on_shutdown()` - lifecycle хуки

### Состояния FSM

#### states.py
Проект использует несколько групп состояний для разных процессов:

```python
# Генерация контента
class ContentGeneration(StatesGroup):
    waiting_for_goal = State()         # Цель контента
    waiting_for_audience = State()     # Целевая аудитория
    waiting_for_platform = State()     # Платформа
    waiting_for_format = State()       # Формат
    waiting_for_has_event = State()    # Наличие события
    waiting_for_event_details = State() # Детали события
    waiting_for_volume = State()       # Объем текста
    waiting_for_user_text = State()    # Дополнительные требования
    waiting_for_confirmation = State() # Подтверждение
    waiting_for_refactoring_text = State() # Редактирование

# Планирование контента
class ContentPlan(StatesGroup):
    waiting_for_period = State()       # Период планирования
    waiting_for_custom_period = State() # Пользовательский период
    waiting_for_frequency = State()    # Частота публикаций
    waiting_for_custom_frequency = State() # Пользовательская частота
    waiting_for_themes = State()       # Темы
    waiting_for_details = State()      # Детали плана

# Управление информацией о НКО
class NGOInfo(StatesGroup):
    waiting_for_ngo_name = State()     # Название НКО
    waiting_for_ngo_description = State() # Описание
    waiting_for_ngo_activities = State() # Деятельность
    waiting_for_ngo_contact = State()  # Контакты
    waiting_for_ngo_confirmation = State() # Подтверждение
```

### Обработчики бота

#### Структура роутеров
```python
# handlers/__init__.py
router = Router(name="main_router")
router.include_router(start_router)           # Старт и приветствие
router.include_router(ngo_info_router)        # Управление НКО
router.include_router(questionnaire_router)   # Анкетирование
router.include_router(generation_router)      # Генерация контента
router.include_router(refactoring_router)     # Редактирование
router.include_router(content_plan_router)    # Планирование
router.include_router(callbacks_router)       # Callback обработчики
router.include_router(fallback_router)        # Обработка ошибок
```

**Основные обработчики:**

1. **start.py** - Команда /start и первичное приветствие
2. **ngo_info.py** - Ввод и редактирование информации о НКО
3. **questionnaire.py** - Сбор данных через анкетирование
4. **generation.py** - Генерация контента и визуальных карточек
5. **refactoring.py** - Редактирование существующего контента
6. **content_plan.py** - Создание контент-планов
7. **callbacks.py** - Обработка inline keyboard callbacks
8. **fallback.py** - Fallback обработчики и обработка ошибок

### Модели данных

#### NGO Model
```python
class NGO(Base):
    """
    Модель для хранения информации об НКО пользователей
    """
    __tablename__ = "ngos"
    
    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, index=True, nullable=False, comment="ID пользователя Telegram")
    
    # Основная информация об НКО
    ngo_name = Column(String(255), nullable=False, comment="Название НКО")
    description = Column(Text, nullable=True, comment="Описание деятельности НКО")
    activities = Column(Text, nullable=True, comment="Формы деятельности НКО")
    contact = Column(Text, nullable=True, comment="Контактная информация")
    
    # Метаданные
    is_active = Column(Boolean, default=True, comment="Активна ли запись")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
```

**Ключевые особенности:**
- Использует SQLAlchemy ORM
- Поддержка автоматических timestamps
- Включает методы сериализации `to_dict()` и `from_dict()`
- Связана с пользователем Telegram через `user_id`
- Индексация по `user_id` для быстрого поиска

### Сервисы

#### TextContentGenerationService
**Основные методы:**
- `generate_text_content()` - генерация нового контента
- `refactor_text_content()` - редактирование существующего контента
- `generate_content_plan()` - создание контент-плана

#### CardGenerationService
- Генерация визуальных карточек с использованием Playwright
- Поддержка различных платформ (Telegram, VK, сайт)
- Интеграция с HTML шаблонами
- Асинхронная обработка множественных запросов

#### NGOService
- Управление данными НКО пользователей
- Создание, обновление, получение информации о НКО
- Валидация данных перед сохранением
- Интеграция с репозиторием и базой данных


## Поток данных

### Генерация контента
```
Пользователь → Questionnaire Handler → Generation Handler 
    ↓
Context Data → TextContentGenerationService → YandexGPT 
    ↓
Raw Response → ResponseProcessor → Generated Text → User
    ↓
Generated Text → CardGenerationService → Playwright → HTML Template
    ↓
Скриншот страницы → Пользоватеь
```

### Управление данными НКО
```
Пользователь → NGO Info Handler → NGOService → NGORepository → Database
    ↓
Database ← Repository ← Service ← Handler ← Validated NGO Data
```

### Планирование контента
```
Пользователь → Content Plan Handler → TextContentGenerationService → YandexGPT
    ↓
Content Plan → ResponseProcessor → Plan Text → User
```

## Зависимости

### Основные библиотеки
- **aiogram** (3.x): Асинхронный фреймворк для Telegram ботов
- **YandexGPT API**: Для генерации контента (актуальная версия)
- **SQLAlchemy**: ORM для работы с базой данных
- **Pydantic**: Валидация данных и настроек
- **Playwright**: Для генерации визуальных карточек
- **aiohttp**: Асинхронные HTTP запросы
- **Jinja2**: Шаблонизация HTML для карточек
- **python-dotenv**: Управление переменными окружения

### Управление зависимостями
- **uv**: Современный менеджер пакетов Python
- **pyproject.toml**: Конфигурация проекта и зависимостей
- **.python-version**: Конкретная версия Python (3.11+)

## Развертывание

### Требования
- Python 3.11+
- SQLite (по умолчанию) или PostgreSQL/MySQL
- драйвер Chromium (для Playwright)
- Доступ к YandexGPT API

### Установка и запуск
1. **Клонирование репозитория:**
   ```bash
   git clone <repository-url>
   cd hackathon
   ```

2. **Установка зависимостей:**
   ```bash
   uv sync
   ```

3. **Настройка переменных окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл с вашими токенами
   ```

4. **Инициализация базы данных:**
   ```bash
   # Автоматическая инициализация при запуске
   python -m src.main
   ```

5. **Установка браузера для Playwright:**
   ```bash
   playwright install chromium
   ```

6. **Запуск приложения:**
   ```bash
   python -m src.main
   ```

### Переменные окружения
```bash
# .env.example
BOT_TOKEN=TG-BOT-TOKEN
YANDEXGPT_API_KEY=YANDEX-API-KEY
YANDEXGPT_CATALOG_ID=YANDEX-CATALOG-ID
DEBUG=False
```

## Масштабирование и расширение

### Добавление нового функционала
1. **Создать модель** в `src/infrastructure/repositories/models/`
2. **Добавить репозиторий** в `src/infrastructure/repositories/`
3. **Реализовать сервис** в `src/services/`
4. **Добавить обработчики** в `src/bot/handlers/`
5. **Обновить состояния** в `src/bot/states.py`
6. **Настроить DI** в `src/bootstrap.py`

### Интеграция с внешними сервисами
1. **Добавить интеграцию** в `src/infrastructure/`
2. **Создать соответствующий сервис**
3. **Обновить конфигурацию**
4. **Добавить обработчики в бота**

