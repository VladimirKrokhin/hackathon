name: Авто-деплой

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Загружаем свежий код
      uses: actions/checkout@v4
      with:
        path: workspace

    - name: Останавливаем существующие контейнеры
      run: |
        echo "🔽 Останавливаем текущие контейнеры..."
        cd workspace/hackathon
        docker-compose down || echo "Контейнеры уже остановлены"

    - name: Собираем Docker образы
      run: |
        echo "🔨 Собираем Docker образы..."
        cd workspace/hackathon
        docker-compose build --parallel

        echo "✅ Образы успешно собраны"

    - name: Запускаем контейнеры
      run: |
        echo "🚀 Запускаем контейнеры..."
        cd workspace/hackathon
        docker-compose up -d

        echo "📦 Контейнеры запущены в фоновом режиме"

    - name: Ждем запуска приложения
      run: |
        echo "⏳ Ждем запуска приложения..."
        cd workspace/hackathon

        # Ждем максимум 60 секунд с проверкой каждые 3 секунды
        MAX_ATTEMPTS=20
        ATTEMPT=1

        while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
          echo "Попытка $ATTEMPT/$MAX_ATTEMPTS: проверка статуса..."

          # Проверяем что контейнер запущен и не завершился с ошибкой
          if docker-compose ps | grep -q "Up"; then
            echo "✅ Контейнер успешно запущен"

            # Дополнительная проверка - что Python процесс работает
            if docker-compose exec -T publichun-bot pgrep -f "python.*main.py" > /dev/null 2>&1; then
              echo "✅ Python процесс найден"
              break
            else
              echo "⏳ Python процесс еще не готов..."
            fi
          else
            echo "⏳ Контейнер еще не готов..."
          fi

          if [ $ATTEMPT -eq $MAX_ATTEMPTS ]; then
            echo "❌ Таймаут: приложение не запустилось"
            docker-compose logs
            exit 1
          fi

          sleep 3
          ATTEMPT=$((ATTEMPT + 1))
        done

    - name: Проверяем здоровье приложения
      run: |
        echo "✅ Проверяем статус развертывания..."
        cd workspace/hackathon
        echo "📊 Статус контейнеров:"
        docker-compose ps

        # Ждем еще немного для полной инициализации
        sleep 5

        echo "📝 Последние логи приложения:"
        docker-compose logs --tail=20 publichun-bot

        echo ""
        echo "🎉 Развертывание завершено успешно!"

    - name: Проверяем доступность сервиса
      run: |
        echo "🔍 Проверяем доступность сервиса..."
        cd workspace/hackathon

        # Проверяем что контейнер запущен
        if docker-compose ps | grep -q "Up"; then
          echo "✅ Контейнеры успешно запущены"
        else
          echo "❌ Ошибка: контейнеры не запустились"
          exit 1
        fi
