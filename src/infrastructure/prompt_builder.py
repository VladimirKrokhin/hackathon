"""
Инфраструктура построения промптов для GPT моделей.

Данный модуль предоставляет структуры данных и построители для создания 
структурированных промптов для различных GPT моделей. Обрабатывает различные 
типы контента, включая посты в социальных сетях, контент-планы и запросы 
на редактирование текста.
"""

from datetime import datetime
from abc import ABC, abstractmethod
from typing import Dict, Union, Iterable, List
import logging
import textwrap

from dtos import PromptContext, PlanPromptContext, EditPromptContext

# Настройка логирования для модуля
logger = logging.getLogger(__name__)


class AbstractPromptBuilder(ABC):
    """
    Абстрактный базовый класс для построения GPT промптов.
    
    Определяет интерфейс для всех построителей промптов, которые создают
    структурированные промпты для различных типов генерации контента.
    """
    
    @abstractmethod
    def build_text_content_prompt(self, user_data: PromptContext, user_text: str) -> str:
        """
        Создает промпт для общей генерации текстового контента.
        
        Args:
            user_data (PromptContext): Данные контекста для генерации контента
            user_text (str): Дополнительный текст пользователя
            
        Returns:
            str: Отформатированный промпт для генерации текстового контента
        """
        pass

    @abstractmethod
    def build_refactor_text_content_prompt(self, user_data: PromptContext, generated_post: str, user_text: str) -> str:
        """
        Создает промпт для рефакторинга текстового контента.
        
        Args:
            user_data (PromptContext): Данные контекста для генерации контента
            generated_post (str): Текстовый контент для рефакторинга
            user_text (str): Запрос пользователя на рефакторинг
            
        Returns:
            str: Отформатированный промпт для рефакторинга текстового контента
        """
        pass

    @abstractmethod
    def build_card_content_prompt(self, user_data: PromptContext, full_content: str) -> str:
        """
        Создает промпт для генерации контента карточки (короткая версия).
        
        Args:
            user_data (PromptContext): Данные контекста для генерации контента
            full_content (str): Полный контент для сокращения для карточки
            
        Returns:
            str: Отформатированный промпт для генерации контента карточки
        """
        pass

    @abstractmethod
    def build_content_plan_prompt(self, user_data: PlanPromptContext) -> str:
        pass

    @abstractmethod
    def build_edit_text_prompt(self, user_data: EditPromptContext) -> str:
        pass


class YandexGPTPromptBuilder(AbstractPromptBuilder):
    """
    Построитель промптов для YandexGPT API.
    
    Создает структурированные промпты для YandexGPT, которые включают данные контекста,
    требования платформы, информацию об НКО и специфические инструкции по генерации контента.
    """

    def build_text_content_prompt(self, user_data: PromptContext, user_text: str) -> str:
        """
        Создает комплексный промпт для генерации контента социальных сетей.
        
        Создает детальный промпт, который включает стиль повествования, требования платформы,
        информацию об НКО и примеры постов для контекста.
        
        Args:
            user_data (PromptContext): Данные контекста, содержащие предпочтения пользователя
            user_text (str): Дополнительное описание или требования пользователя
            
        Returns:
            str: Отформатированный промпт для генерации контента
        """
        sections = []
        
        # Определяем стиль повествования
        narrative_style = self._get_narrative_style(user_data.narrative_style)
        sections.append(f"Стиль повествования: {narrative_style}")

        # Используем данные структурированной формы, если доступны
        if user_data.event_type or user_data.event_date:
            sections.extend([
                "Используйте структурированные данные для создания поста:",
                f"• Мероприятие: {user_data.event_type}",
                f"• Дата и время: {user_data.event_date}",
                f"• Место: {user_data.event_place}",
                f"• Целевая аудитория: {user_data.event_audience}",
                f"• Дополнительные детали: {user_data.event_details}",
            ])
        else:
            # Используем пользовательское описание
            sections.append("Информация пользователя:")
            sections.append(user_text.strip())

        # Добавляем информацию об НКО, если доступна
        if user_data.has_ngo_info and user_data.ngo_name:
            sections.extend([
                "Информация об НКО:",
                f"• Название: {user_data.ngo_name}",
            ])
            if user_data.ngo_description and user_data.ngo_description != "Не указано":
                sections.append(f"• Описание: {user_data.ngo_description}")
            if user_data.ngo_activities and user_data.ngo_activities != "Не указано":
                sections.append(f"• Деятельность: {user_data.ngo_activities}")
            if user_data.ngo_contact and user_data.ngo_contact != "Не указано":
                sections.append(f"• Контакты: {user_data.ngo_contact}")
            sections.append("Обязательно используйте эту информацию при создании поста.")

        sections.extend([
            f"Платформа: {user_data.platform} ({self._get_platform_requirements(user_data.platform)})",
            "Дополнительные требования:",
            "• Не упоминать режимные объекты, безопасность, военные базы или ограничения на передвижение.",
            "• Фокусироваться на социальной миссии и помощи людям.",
            "• Обязательно добавить контактную информацию и призыв к конкретному действию.",
            "• ОБЯЗАТЕЛЬНО добавить 3-5 релевантных хештегов в конце поста по теме мероприятия/поста.",
        ])

        sections.append("Примеры успешных постов для вдохновения:")
        sections.extend(
            [
                '• "🌟 Друзья, нам нужны волонтеры! Помогите детям из многодетных семей подготовиться к школе. Ваше участие изменит жизни этих детей!"',
                '• "💡 Ваша поддержка важна! Благодаря вам мы собрали 100 наборов школьных принадлежностей для детей в нашем городе."',
                '• "Присоединяйтесь к нам на благотворительном концерте! 15 декабря в ДК \"Родина\". Все средства пойдут на ремонт детской площадки."',
            ]
        )

        sections.append(
            "Отвечайте только готовым текстом поста, без дополнительных комментариев и пояснений."
        )

        prompt = "\n".join(sections)
        return textwrap.dedent(prompt).strip()

    def build_card_content_prompt(self, user_data: PromptContext, full_content: str) -> str:
        """
        Создает промпт для генерации короткого контента карточки (1-2 предложения).
        
        Создает промпт, специально предназначенный для извлечения сути
        из полного поста и создания краткой версии для отображения на карточках.
        
        Args:
            user_data (PromptContext): Данные контекста для генерации контента
            full_content (str): Полный текст поста для сокращения
            
        Returns:
            str: Отформатированный промпт для генерации контента карточки
        """
        sections = [
            "Задача: создать короткий контент для информационной карточки на основе полного поста.",
        ]

        # Информация о платформе и требования
        platform_requirements = self._get_platform_requirements(user_data.platform)
        sections.append(f"Платформа карточки: {user_data.platform} ({platform_requirements})")

        # Требования к контенту карточки
        sections.extend([
            "Требования к контенту карточки:",
            "• Максимум 1 короткое предложение (4-8 слов)",
            "• Должен содержать суть и призыв к действию",
            "• Краткий, но привлекательный текст",
            "• Без хештегов (они будут добавлены отдельно)",
            "• Поддерживать стиль повествования: позитивный, вдохновляющий",
        ])

        # Добавляем информацию об НКО для контекста
        if user_data.has_ngo_info and user_data.ngo_name:
            sections.extend([
                "\nИнформация об НКО:",
                f"• Название: {user_data.ngo_name}",
            ])
            if user_data.ngo_description and user_data.ngo_description != "Не указано":
                sections.append(f"• Миссия: {user_data.ngo_description}")

        sections.extend([
            "\nВот полный текст поста для сокращения:",
            full_content,
            "\nСоздайте короткую версию этого поста для информационной карточки (1-2 предложения).",
            "Отвечайте только сокращенным текстом, без дополнительных комментариев."
        ])

        prompt = "\n".join(sections)
        return textwrap.dedent(prompt).strip()

    def build_refactor_text_content_prompt(self, user_data: PromptContext, generated_post: str, user_text: str) -> str:
        """
        Создает промпт для рефакторинга и редактирования текстового контента.
        
        Создает промпт для изменения существующего контента согласно
        требованиям пользователя, сохраняя контекст и стиль.
        
        Args:
            user_data (PromptContext): Данные контекста для генерации контента
            generated_post (str): Текстовый контент для рефакторинга
            user_text (str): Запрос пользователя на изменения при рефакторинге
            
        Returns:
            str: Отформатированный промпт для рефакторинга контента
        """
        narrative_style = self._get_narrative_style(user_data.narrative_style)
        
        sections = [
            "Вы профессиональный SMM-менеджер НКО.",
            f"Отредактируйте пост согласно этому запросу: {user_text}",
            f"Стиль повествования: {narrative_style}",
        ]

        # Добавляем структурированные данные, если доступны
        if user_data.event_type or user_data.event_date:
            sections.extend([
                "Структурированные данные:",
                f"• Мероприятие: {user_data.event_type}",
                f"• Дата: {user_data.event_date}",
                f"• Место: {user_data.event_place}",
                f"• Аудитория: {user_data.event_audience}",
            ])

        # Добавляем информацию об НКО
        if user_data.has_ngo_info and user_data.ngo_name:
            sections.extend([
                "Информация об НКО:",
                f"• Название: {user_data.ngo_name}",
            ])
            if user_data.ngo_description and user_data.ngo_description != "Не указано":
                sections.append(f"• Описание: {user_data.ngo_description}")
            if user_data.ngo_activities and user_data.ngo_activities != "Не указано":
                sections.append(f"• Деятельность: {user_data.ngo_activities}")
            if user_data.ngo_contact and user_data.ngo_contact != "Не указано":
                sections.append(f"• Контакты: {user_data.ngo_contact}")

        sections.extend([
            f"Платформа: {user_data.platform} ({self._get_platform_requirements(user_data.platform)})",
            "Дополнительные требования:",
            "• Не упоминать режимные объекты, безопасность, военные базы или ограничения на передвижение.",
            "• Фокусироваться на социальной миссии и помощи людям.",
            "• Обязательно добавить контактную информацию и призыв к конкретному действию.",
            "• ОБЯЗАТЕЛЬНО добавить 3-5 релевантных хештегов в конце поста.",
        ])

        sections.append("Вот пост для редактирования:")
        sections.append(generated_post)

        sections.append(
            "Отвечайте только готовым отредактированным текстом поста, без дополнительных комментариев. "
            "Не используйте одиночные звездочки '*' и подчеркивания '_'!"
        )
        
        prompt = "\n".join(sections)
        return textwrap.dedent(prompt).strip()

    def build_content_plan_prompt(self, user_data: PlanPromptContext) -> str:
        """
        Создает промпт для планирования контента.

        Args:
            user_data (PlanPromptContext): Данные контекста для планирования контента

        Returns:
            str: Отформатированный промпт для генерации контент-плана
        """
        period = user_data.period
        frequency = user_data.frequency
        themes = user_data.themes
        details = user_data.details

        sections = [
            "Задача: создать контент-план для аккаунта НКО в социальных сетях. Укажите дни и категории постов.",
            "Контекст для создания:",
            f"• Период: {period}",
            f"• Частота публикаций: {frequency}",
            f"• Темы: {themes}",
        ]

        if details:
            sections.append(f"• Специальные требования: {details}")

        sections.extend([
            "Также предоставьте объяснения, почему вы предложили именно такой план.",
            "Используйте отступы, списки и эмодзи для лучшей читаемости. Не перегружайте текст.",
            "Используйте форматирование текста, которое корректно отображается в Telegram.",
        ])

        prompt = "\n".join(sections)
        return textwrap.dedent(prompt).strip()

    def build_structured_content_plan_prompt(self, user_data: PlanPromptContext) -> str:
        """
        Создает промпт для генерации комплексных контент-планов,
        включая расписания публикаций, темы и стратегические рекомендации.
        
        Args:
            user_data (PlanPromptContext): Данные контекста для планирования контента
            
        Returns:
            str: Отформатированный промпт для генерации контент-плана
        """
        period = user_data.period
        frequency = user_data.frequency
        themes = user_data.themes
        details = user_data.details

        sections = [
            "Твоя задача — составить детальный контент-план для НКО в социальной сети, основываясь на вводных данных.",
            "ВВОДНЫЕ ДАННЫЕ:",
            f"- Период планирования: {period}",
            f"- Частота публикаций: {frequency}",
            f"- Темы для плана: {themes}",
            f"- Текущие дата и время (год-месяц-день часы:минуты:секунды): {datetime.today().strftime('%Y-%m-%d %H:%M:%S')}"
        ]

        if details:
            sections.append(f"- Дополнительная информация и пожелания: {details}")

        sections.append(
            "ТРЕБОВАНИЯ К КОНТЕНТУ:"
            "1. Описания постов должны быть грамотными и соответствовать теме."
            "2. Заголовки должны быть кликбейтными, но релевантными."
            "3. Даты публикаций должны логично распределяться "
            f"внутри указанного периода ({period}) согласно частоте ({frequency})."
            f""
            f"ФОРМАТ ОТВЕТА:"
            "Верни результат СТРОГО в формате JSON. "
            "Не используй разметку Markdown (без ```json). Не пиши никаких вводных слов или объяснений."
            "## Ответ должен представлять собой список (массив) объектов. "
            "Каждый объект соответствует одной публикации и содержит следующие ключи:"
            '- "publication_date": дата и время публикации в формате ISO (YYYY-MM-DDTHH:MM:SS).'
            '- "content_title": заголовок поста.'
            '- "content_text": детальное описание наполнения поста, но не сам пост!'
            '- "status": всегда используй значение "запланировано".'
            
            "## Пример структуры JSON (заполни его своими данными):"
            '['
            '  {'
            '    "publication_date": "2023-10-25T10:00:00",'
            '    "content_title": "Заголовок поста",'
            '    "content_text": "Описание поста...",'
            '    "status": "запланировано"'
            '  }'
            ']'
        )

        prompt = "\n".join(sections)
        return textwrap.dedent(prompt).strip()

    def build_edit_text_prompt(self, user_data: EditPromptContext) -> str:
        """
        Создает промпт для редактирования и вычитки текста.
        
        Создает промпт для грамматического, логического и стилистического
        улучшения текста, сохраняя при этом основную тему.
        
        Args:
            user_data (EditPromptContext): Данные контекста для редактирования текста
            
        Returns:
            str: Отформатированный промпт для редактирования текста
        """
        text_to_edit = user_data.text_to_edit
        details = user_data.details

        sections = [
            "Задача: отредактировать текст на грамматику, орфографию, логику и стиль. ",
            "Постараться сохранить основную тему исходного текста. ",
            "Показать исправленную версию и перечислить, какие ошибки были найдены и как они исправлены.",
            f"Исходный текст: {text_to_edit}",
        ]

        # Добавляем информацию об НКО, если доступна
        if user_data.has_ngo_info and user_data.ngo_name:
            sections.extend([
                "",
                "Информация об НКО (используйте эту информацию при редактировании, если она релевантна):",
                f"• Название: {user_data.ngo_name}",
            ])
            if user_data.ngo_description and user_data.ngo_description != "Не указано":
                sections.append(f"• Описание: {user_data.ngo_description}")
            if user_data.ngo_activities and user_data.ngo_activities != "Не указано":
                sections.append(f"• Деятельность: {user_data.ngo_activities}")
            if user_data.ngo_contact and user_data.ngo_contact != "Не указано":
                sections.append(f"• Контакты: {user_data.ngo_contact}")
            sections.append("Если в тексте упоминается организация, используйте информацию об НКО для уточнения.")

        if details:
            sections.append(f"Дополнительные детали для рассмотрения при редактировании текста: {details}")

        prompt = "\n".join(sections)
        return textwrap.dedent(prompt).strip()

    @staticmethod
    def _normalize_to_list(value: Union[str, Iterable[str]]) -> List[str]:
        """
        Нормализует значение в список строк, игнорируя значения None.
        
        Args:
            value (Union[str, Iterable[str]]): Значение для нормализации
            
        Returns:
            List[str]: Нормализованный список строк
        """
        if value is None:
            return []
        if isinstance(value, str):
            value = value.strip()
            return [value] if value else []
        if isinstance(value, Iterable):
            result = []
            for item in value:
                if item is None:
                    continue
                if isinstance(item, str):
                    item = item.strip()
                    if item:
                        result.append(item)
                else:
                    result.append(str(item))
            return result
        return [str(value)]

    @staticmethod
    def _get_narrative_style(style: str) -> str:
        """
        Определяет стиль повествования на основе выбора пользователя.
        
        Args:
            style (str): Предпочтение стиля, выбранное пользователем
            
        Returns:
            str: Подробное описание стиля для промпта
        """
        style_lower = style.lower()
        
        if "разговорный" in style_lower:
            return "дружелюбный, простой язык, как разговор с близким другом, используй личные местоимения и эмодзи"
        elif "официально-деловой" in style_lower:
            return "профессиональный, структурированный, официальный тон, четкие формулировки"
        elif "художественный" in style_lower:
            return "образный, эмоциональный, используй метафоры и яркие описания"
        elif "позитивный" in style_lower or "мотивирующий" in style_lower:
            return "вдохновляющий, энергичный, с акцентом на положительные эмоции и возможности"
        else:
            return "универсальный, дружелюбный, но профессиональный"

    @staticmethod
    def _get_audience_style(audience: List[str]) -> str:
        """
        Определяет стиль текста на основе целевой аудитории.
        
        Args:
            audience (List[str]): Список сегментов аудитории
            
        Returns:
            str: Описание стиля, подходящего для аудитории
        """
        audience_lower = ", ".join(audience).lower()

        if any(key in audience_lower for key in ("молодежь", "14-25", "студенты")):
            return "неформальный, энергичный, с эмодзи в начале абзацев, современный сленг"
        if any(key in audience_lower for key in ("семьи с детьми", "родители")):
            return "теплый, заботливый, без жаргона, с акцентом на семейные ценности"
        if any(key in audience_lower for key in ("бизнес", "организации", "компании")):
            return (
                "профессиональный, с акцентом на социальную ответственность и измеримые результаты"
            )
        if any(key in audience_lower for key in ("пожилые", "45+", "старшее поколение")):
            return "уважительный, понятный, без сложных терминов, с акцентом на традиции и заботу"
        return "универсальный, дружелюбный, но профессиональный"

    @staticmethod
    def _get_platform_requirements(platform: str) -> str:
        """
        Получает требования к форматированию для конкретной платформы.
        
        Args:
            platform (str): Целевая платформа социальных сетей
            
        Returns:
            str: Инструкции по форматированию для конкретной платформы
        """
        platform_lower = platform.lower()

        if any(key in platform_lower for key in ("вконтакте", "vk", "вк")):
            return (
                "3-5 эмодзи в тексте, 3-5 релевантных хештегов в конце, короткие абзацы (1-2 предложения)"
            )
        if any(key in platform_lower for key in ("telegram", "телеграм", "tg")):
            return (
                "используй **жирный** для заголовков, --- для разделителей, 1-2 ключевых хештега, минимум эмодзи"
            )
        if any(key in platform_lower for key in ("сайт", "рассылка", "newsletter")):
            return "официальный стиль, полные предложения, без эмодзи"
        return "универсальные требования для социальных сетей"

    @staticmethod
    def _format_event_details(event_details: Union[str, Dict]) -> str:
        """
        Форматирует детали мероприятия для включения в промпт.
        
        Args:
            event_details (Union[str, Dict]): Детали мероприятия для форматирования
            
        Returns:
            str: Отформатированные детали мероприятия с инструкциями шаблона
        """
        if isinstance(event_details, dict):
            parts = []
            for key, value in event_details.items():
                if value:
                    parts.append(f"{key.capitalize()}: {value}")
            event_text = "; ".join(parts)
        else:
            event_text = str(event_details).strip()

        template = textwrap.dedent(
            """
            • Укажите точную дату и время.
            • Укажите место (без упоминания закрытых территорий).
            • Объясните, зачем нужно это мероприятие и что будет интересного.
            • Расскажите, что получат участники.
            • Добавьте срочный призыв к регистрации, если есть дедлайн.
            """
        ).strip()

        return f"{event_text}\n{template}"

